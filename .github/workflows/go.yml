name: Go

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main" ]
    
jobs:
  go-pipeline:
    uses: The-Psyducks/ci-template/.github/workflows/lintTest&RunContainer.yml@main

